description: 'Single step workflow'
parameters:
    message:
        required: true
        type: string
steps:
    - name: notify
      link: test-slack
      parameters:
          message: '{message}'
