parameters:
    greeting:
        required: false
        type: string
        default: 'Hello'
    name:
        required: false
        type: string
        default: 'World'

message_template: '{greeting}, {name}!'

channels:
    - transport: slack
